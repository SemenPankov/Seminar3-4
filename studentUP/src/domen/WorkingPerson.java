package domen;

/**
 * класс для доступа к конкретным типам для защиты типов
 */
public class WorkingPerson extends Person{

    public WorkingPerson(String name, int age) {
        super(name, age);
    }
}
